# خطة التنفيذ الكاملة لمنصة بريد عرجون

آخر تحديث: 24 سبتمبر 2026 (تنفيذ الشريحة الأولى: الهيكل الأحادي والخلفية وJWT والخدمات والطلبات والمتابعة والرسائل والمحتوى).

هذا الملف هو مرجع تنفيذ المشروع للمطوّر أو وكيل الذكاء الاصطناعي. يضم المتطلبات المتفق عليها، المهارات المطلوبة، حالة التنفيذ، المراحل المتبقية ومعايير القبول. القرارات الأحدث التي يصدرها صاحب المشروع مقدّمة على هذا الملف، ويجب تحديثه عند اعتماد تغيير.

> الحالة الحالية: المستودع أحادي فعليًا. خلفية Django وDRF على PostgreSQL تعمل مع مصادقة JWT وصلاحيات الأدوار، وNext.js في `frontend/` بوضع standalone يمرّر API إلى Django. الخدمات والطلبات والمتابعة والرسائل والإعدادات والأسئلة الشائعة والفريق مربوطة بقاعدة البيانات، والصفحات العامة مرندرة على الخادم مع SEO وإبطال كاش. لم يُنفّذ بعد: المرفقات والوسائط، التسعير والدفع، تنبيهات البريد، الصفحات والأقسام المخصصة، النسخ الاحتياطي، والنشر على Render. التفاصيل في القسم 4 و`docs/`.

## 1. الهدف والقرارات المعتمدة

بريد عرجون منصة خدمات متنوعة. صاحب المنصة يحدد المجالات والخدمات التي يعرضها ومتطلبات طلب كل خدمة. البريد والتعليم والسفر أمثلة وليست مجالات ثابتة أو حصرية.

### رحلة العميل

1. يتصفح المجالات والخدمات المنشورة ويقرأ التفاصيل والمتطلبات وطريقة التسعير.
2. يملأ نموذج الخدمة ويُدخل اسمه ورقم هاتفه المستخدم في WhatsApp مع رمز الدولة.
3. يرفق الملفات المطلوبة إذا كانت الخدمة تستلزمها.
4. يرسل الطلب ويحصل على رقم متابعة، مع زر لنسخه وتعليمات للاحتفاظ به.
5. يستخدم رقم المتابعة لمعرفة حالة الطلب والملاحظات العامة دون إنشاء حساب.
6. يتواصل معه المسؤول عبر WhatsApp عند الحاجة إلى معلومات أو موافقة.

### رحلة المسؤول

1. يُحفظ الطلب أو الاستفسار في النظام، ثم يصل إشعار إلى بريد الإدارة برابط لفتحه.
2. يدخل المسؤول إلى لوحة التحكم لمراجعة التفاصيل والمرفقات.
3. يستطيع فتح محادثة WhatsApp مع رقم العميل من زر داخل الطلب أو الرسالة.
4. يسند الطلب ويحدّث حالته ويسجل السعر والدفع والملاحظات بحسب الصلاحية.
5. يظهر للعميل تحديث الحالة والملاحظات العامة فقط.

### توزيع قنوات التواصل

- الموقع: تقديم الطلبات والاستفسارات ومتابعة الحالة.
- لوحة التحكم: المكان الأساسي لإدارة العمل والبيانات.
- البريد الإلكتروني: تنبيهات للإدارة، وليس صندوق محادثات داخل المنصة.
- WhatsApp: تواصل مباشر يفتحه المستخدم أو المسؤول. فتح المحادثة لا يرسل الرسالة تلقائيًا.
- لا توجد حسابات للعملاء أو محادثات داخل الموقع في النسخة الأولى.

## 2. التقنية ونظام التصميم

| المكوّن | القرار |
| --- | --- |
| نمط البنية | نظام أحادي (Monolithic): مستودع واحد، مشروع Django واحد، نشر واحد |
| الموقع ولوحة التحكم | Next.js + TypeScript داخل مشروع Django (مجلد `frontend/`) بوضع الخادم (`output: 'standalone'`) مع رندرة الصفحات العامة على الخادم |
| الخلفية | Django + Django REST Framework تحت `/api/v1/` على الأصل نفسه |
| المصادقة | JWT عبر `djangorestframework-simplejwt`: access في الذاكرة، refresh في httpOnly cookie |
| حالة الواجهة | React Context API فقط (مصادقة وواجهة)، بلا Redux أو Zustand |
| بيانات الخادم في الواجهة | TanStack Query (React Query v5) لكل استعلام وتعديل عبر API |
| قاعدة البيانات | PostgreSQL |
| الاستضافة المستهدفة | Render: خدمة ويب واحدة تشغّل Next.js وGunicorn معًا + PostgreSQL + قرص دائم |
| الملفات | Render Persistent Disk بسعة 10 GB مرتبط بخدمة Django |
| الأيقونات | lucide-react |
| اللغة والاتجاه | العربية وRTL |
| الخط | Noto Sans Arabic مع بدائل مناسبة |

استخدم إصدارات مستقرة ومتوافقة، وثبّت الاعتماديات بملفات قفل. لا تغيّر التقنية المتفق عليها لمجرد اختصار العمل. الإصدارات الموجودة في package.json تمثل المعاينة الحالية؛ راجع ملاءمتها وأمنها عند مرحلة التشغيل.

### قرارات البنية الملزمة

هذه القرارات معتمدة من صاحب المشروع ولا تُغيَّر دون قرار صريح منه.

**1. نظام أحادي (Monolithic)**

- مستودع واحد ومشروع Django واحد وخدمة Render واحدة وعملية نشر واحدة. لا خدمات مصغرة، ولا خدمة Render منفصلة للواجهة، ولا API Gateway.
- داخل الخدمة الواحدة تعمل عمليتان يشغّلهما سكربت بدء واحد: خادم Next.js (المنفذ المكشوف) وGunicorn لـ Django (منفذ داخلي على localhost). فشل أي منهما يُسقط فحص الصحة ويعيد Render تشغيل الخدمة كاملة.
- الخلفية والواجهة والملفات على الأصل نفسه (نفس النطاق والمنفذ المكشوف). لا يوجد CORS في الإنتاج ولا في التطوير، لأن Next يمرّر طلبات API إلى Django داخليًا.
- تُقسَّم الخلفية إلى تطبيقات Django منطقية داخل المشروع نفسه، لا إلى خدمات مستقلة.

**2. Next.js داخل مشروع DRF**

- يعيش تطبيق Next.js في مجلد `frontend/` داخل جذر مشروع Django. تُنقل إليه ملفات `app/` و`public/` و`package.json` و`next.config.ts` و`tsconfig.json` الحالية دون تغيير المظهر المعتمد.
- يُبنى Next.js بوضع الخادم `output: 'standalone'` ويعمل بـ `next start` على المنفذ المكشوف. لا تصدير ثابت. قرار التصدير الثابت أُلغي لأنه يترك محتوى صفحات الخدمات فارغًا في HTML الأولي، ولا يعرف الخدمات المضافة من الإدارة، ولا يرجع 404 حقيقيًا، ويعطّل تحسين الصور.
- `rewrites` في `next.config.ts` تمرّر `/api/*` و`/media/*` و`/django-admin/*` (إن فُعِّل) إلى Django على `http://127.0.0.1:8000` في التطوير والإنتاج معًا. المتصفح لا يرى Django مباشرة أبدًا.
- الصفحات العامة (الرئيسية، قائمة الخدمات، تفاصيل الخدمة، الصفحات الثابتة، الأسئلة الشائعة) تُرندر على الخادم بـ Server Components تجلب من Django عبر عنوان داخلي (`INTERNAL_API_URL`) مع `revalidate` قصير (60 ثانية) ووسوم كاش (`next: { tags }`). عند النشر أو التعديل من لوحة التحكم تستدعي Django مسارًا محميًا في Next لإبطال الوسم فورًا (`revalidateTag`). المحتوى كامل في HTML الأولي، و`generateMetadata` تولّد العنوان وmeta وOG وcanonical من البيانات نفسها، وخدمة غير موجودة ترجع `notFound()` بكود 404 حقيقي.
- لوحة التحكم وصفحة المتابعة ونماذج الطلب تبقى مكوّنات عميل تعتمد TanStack Query وContext API كما هو محدد أدناه، مع `robots: noindex`. لا يُستخدم Server Components لأي بيانات إدارية أو خاصة.
- Route Handlers في Next مسموحة لثلاثة أغراض فقط: `sitemap.xml` و`robots.txt` ومسار إبطال الكاش. لا منطق أعمال ولا وصول لقاعدة البيانات من Next. Server Actions وmiddleware غير مستخدمة في النسخة الأولى.
- `next/image` مفعّل للصور العامة من `/media/` عبر `remotePatterns`، مع نص بديل إلزامي من مكتبة الوسائط.
- الخط العربي يُستضاف محليًا عبر `next/font/local` بدل Google Fonts لتحسين LCP على الهاتف.
- ملفات Django الثابتة (`/django-admin/` و`/media/` العام) تُقدَّم عبر WhiteNoise من Django خلف تمرير Next. المرفقات الخاصة لا تمر عبر `/media/` بل عبر روابط موقّعة.
- سكربت البدء `start.sh`: يشغّل `gunicorn` على 8000 ثم `node frontend/.next/standalone/server.js` على `$PORT`، وينهي الاثنين عند خروج أي منهما. سكربت البناء `build.sh`: `npm ci && npm run build` داخل `frontend/` ثم `collectstatic` ثم `migrate`.
- الاعتماديات الخلفية المعتمدة: Django، djangorestframework، djangorestframework-simplejwt، django-filter، drf-spectacular، whitenoise، psycopg، gunicorn، django-environ (أو ما يعادلها لقراءة البيئة). لا حاجة إلى `django-cors-headers`.
- الاعتماديات الأمامية المضافة: `@tanstack/react-query` و`@tanstack/react-query-devtools` (تطوير فقط).

**3. JWT للمصادقة والتفويض**

- مكتبة `djangorestframework-simplejwt`. الرمزان: access قصير العمر (نحو 15 دقيقة) وrefresh أطول (نحو 7 أيام) مع تدوير (rotation) وقائمة إبطال (blacklist).
- يُحفظ access في ذاكرة التطبيق فقط داخل `AuthContext`. لا يُكتب أبدًا في localStorage أو sessionStorage أو cookie قابل للقراءة من JavaScript.
- يُحفظ refresh في cookie بخصائص `HttpOnly` و`Secure` و`SameSite=Strict` ومسار محصور بمسارات المصادقة `/api/v1/auth/`. نقاط الدخول والتجديد والخروج هي الوحيدة التي تقرأ هذا الـ cookie.
- الخروج يبطل refresh على الخادم ويمسح الـ cookie ويصفّر الحالة في الواجهة ويُفرغ كاش TanStack Query.
- عميل API الموحد يضيف `Authorization: Bearer` لكل طلب إداري، وعند 401 يحاول التجديد مرة واحدة ثم يعيد الطلب الأصلي؛ إن فشل التجديد يخرج المستخدم ويحفظ المدخلات غير المرسلة محليًا في الذاكرة إلى حين إعادة الدخول.
- واجهات API العامة (الخدمات المنشورة، إنشاء الطلب، المتابعة، الاستفسار) بلا مصادقة، مع تقييد معدل الطلبات (throttling) لكل عنوان IP.
- تنزيل المرفقات الخاصة لا يعتمد على ترويسة Authorization لأن روابط التنزيل المباشرة لا تحملها. يطلب المستخدم عبر API (بـ JWT) رابط تنزيل موقّعًا قصير الأجل تتحقق منه Django قبل تقديم الملف.
- الصلاحيات (مدير، مشغّل، منفذ) تُقرأ من المستخدم في قاعدة البيانات عند كل طلب وليس من محتوى الرمز فقط، حتى ينعكس تغيير الدور أو التعطيل فورًا.
- نقاط المصادقة التي تعتمد على cookie تُحمى من CSRF بفحص ترويسة Origin/Referer إضافة إلى `SameSite=Strict`. بقية واجهات API لا تستخدم جلسات Django ولا CSRF token. تمرير Next يجب أن يحفظ ترويسات `Cookie` و`Set-Cookie` و`Origin` كما هي، ويُتحقق من ذلك في المرحلة 3.

**4. Context API لحالة الواجهة**

- `AuthProvider`: المستخدم الحالي ودوره، رمز access، دوال الدخول والخروج والتجديد الصامت، حالة التحميل الأولي.
- `UiProvider`: التنبيهات المؤقتة (toasts)، النوافذ الحوارية، حالة القوائم الجانبية، أي تفضيلات عرض محلية.
- لا تُخزَّن بيانات الخادم (طلبات، خدمات، رسائل، إعدادات) في أي Context؛ مكانها TanStack Query فقط. المزوّدات تُركَّب في `app/layout.tsx` بترتيب: `QueryClientProvider` ثم `AuthProvider` ثم `UiProvider`.
- عميل API يقرأ رمز access من مخزن وحدة (module store) يحدّثه `AuthProvider`، لتجنب تمرير الرمز يدويًا لكل استدعاء.

**5. TanStack Query لبيانات الخادم**

- كل قراءة من API عبر `useQuery`/`useInfiniteQuery`، وكل كتابة عبر `useMutation`. لا `useEffect` + `fetch` يدوي للبيانات.
- مفاتيح الاستعلام مركزية في ملف `queryKeys` بشكل هرمي (مثل `['orders', 'list', filters]` و`['orders', 'detail', id]`) لتسهيل الإبطال.
- بعد كل تعديل ناجح: إبطال المفاتيح المتأثرة أو تحديث الكاش مباشرة. التحديث التفاؤلي مسموح لتغيير حالة الطلب والرسائل مع تراجع عند الفشل.
- إعدادات افتراضية: `staleTime` قصير للوحة التحكم (نحو 30 ثانية) وأطول للمحتوى العام؛ `retry` محدود ولا يُعاد عند 4xx؛ `refetchOnWindowFocus` مفعّل في لوحة التحكم.
- حالات التحميل والخطأ والفراغ وإعادة المحاولة تُبنى على حالة الاستعلام نفسها، لا على متغيرات محلية موازية.
- أدوات المطوّر (Devtools) في بيئة التطوير فقط. لا استمرار (persist) للكاش في localStorage في النسخة الأولى.
- الصفحات العامة المرندرة على الخادم لا تستخدم TanStack Query لبياناتها الأولية. إن احتاج جزء تفاعلي فيها إلى بيانات حية (مثل حالة نشر خدمة أثناء التصفح) يُمرَّر الجلب الأولي كـ `initialData` إلى الاستعلام ذاته لتفادي طلب مكرر.

**6. قواعد SEO الملزمة**

- كل خدمة لها `slug` ثابت فريد يُولَّد عند الإنشاء ويُحفظ؛ تغييره يسجّل القديم في جدول تحويلات ويُرجع 301 من Next عبر `redirects` ديناميكية أو من الصفحة نفسها.
- كل صفحة عامة لها `canonical` وعنوان ووصف قابلان للتحرير من الإدارة، مع قيم افتراضية مشتقة من المحتوى.
- بيانات منظمة JSON-LD: `Organization` في التخطيط العام، `Service` و`BreadcrumbList` في صفحة الخدمة، `FAQPage` حيث توجد أسئلة شائعة.
- `sitemap.xml` و`robots.txt` تُولَّدان من Next بجلب الخدمات والصفحات المنشورة فقط، وتستبعد المسودات ولوحة التحكم والمتابعة وصفحات النجاح.
- صفحات الإدارة والمتابعة والنجاح تحمل `noindex, nofollow` في metadata وفي `robots.txt`.
- الصور العامة عبر `next/image` بنص بديل إلزامي وأبعاد معروفة لتفادي تحرك التخطيط.
- أهداف الأداء على الهاتف: LCP أقل من 2.5 ثانية وCLS أقل من 0.1 على صفحة خدمة، تُقاس بـ Lighthouse قبل النشر.

### الهوية البصرية

| الاستخدام | اللون |
| --- | --- |
| الأساسي والأزرار الرئيسية | `#145A46` |
| خلفية الموقع | `#FAF8F2` |
| البطاقات والنماذج | `#FFFFFF` |
| خلفيات مساندة | `#DDEBE3` |
| إبراز ذهبي محدود | `#D9A441` |
| النص الرئيسي | `#202923` |
| الحدود والفواصل | `#E5E7E2` |

- حافظ على الاتجاه البصري الذي أعجب صاحب المشروع: هدوء، وضوح، ومساحات مريحة وزوايا مستديرة باعتدال.
- استخدم lucide-react للأيقونات الوظيفية، مع أحجام وسماكات موحدة وتسميات وصول للأزرار التي تحتوي أيقونة فقط.
- صمّم للهاتف أولًا، مع دعم الحاسوب واللوحي والنصوص العربية المختلطة بالأرقام.
- لا تكتفِ باللون لتوضيح حالة الطلب. تحقق من التباين والتركيز ولوحة المفاتيح.
- لا تربط مجالًا بلون أو عدد ثابت في الكود.
- الصور والشعار الحاليان اتجاه أولي قابل للتعديل، وليسا إعلانًا عن اعتماد شعار نهائي.

## 3. المهارات المطلوبة للوكيل المنفّذ

هذه تخصصات عمل، ولا تعني أن إضافات بهذه الأسماء مثبتة. افحص الأدوات والمهارات الفعلية واستخدم المناسب دون اختلاق أسماء أدوات أو تثبيت إضافات غير لازمة.

| المهارة | موضع استخدامها |
| --- | --- |
| تحليل المتطلبات وتصميم الأنظمة | نموذج البيانات، الرحلات، العقود بين الواجهة والخلفية |
| UX/UI عربي وRTL وسهولة الوصول | استكمال الواجهات والتجاوب والنماذج وحالات الخطأ |
| Next.js وReact وTypeScript | App Router بوضع الخادم، Server Components للصفحات العامة، Context API لحالة الواجهة، TanStack Query للوحة التحكم |
| lucide-react ونظام التصميم | أيقونات متناسقة وإشارات بصرية مفهومة |
| Django وDRF | منطق الأعمال، التحقق، JWT بـ simplejwt، الصلاحيات، إبطال كاش Next عند النشر |
| PostgreSQL | العلاقات والترحيلات والفهارس والقيود وحماية السجل التاريخي |
| أمن تطبيقات الويب | JWT وrefresh cookie وحماية نقاط المصادقة من CSRF وروابط التنزيل الموقّعة وتقييد إساءة الاستخدام |
| الملفات والوسائط | التحقق من النوع والحجم والتخزين العام والخاص والاحتفاظ |
| البريد والمهام الخلفية | التنبيهات وسجل الإرسال وإعادة المحاولة |
| الاختبارات ومراجعة المتصفح | دورة الطلب والصلاحيات والفشل والعرض المتجاوب |
| Render والتشغيل | النشر والقرص الدائم وفحوص الصحة والنسخ والاستعادة |
| التوثيق | تشغيل المشروع وتسليمه ودليل الإدارة وسجل القرارات |

استعن بالوثائق الرسمية للمعلومات التي تتغير، خاصة إعدادات Render وإصدارات المكتبات. لا تنشئ موارد مدفوعة أو تنشر للعامة دون تفويض.

## 4. حالة المشروع الحالية بدقة

### الموجود في الملفات

| المسار | المحتوى |
| --- | --- |
| `config/`، `apps/` | مشروع Django وتطبيقاته السبعة (راجع `docs/architecture.md`) |
| `apps/*/tests/` | 43 اختبارًا خلفيًا لمصفوفة القبول الحرجة |
| `frontend/app/(site)/` | الصفحات العامة مرندرة على الخادم |
| `frontend/app/admin/` | لوحة التحكم: الدخول، نظرة عامة، الطلبات، الخدمات، الرسائل، المحتوى والإعدادات، الفريق |
| `frontend/src/` | عميل API، Context، hooks فوق TanStack Query، المكوّنات |
| `frontend/src/legacy/` | المعاينة الأصلية كمرجع تصميم للشاشات غير المربوطة |
| `build.sh`، `start.sh`، `render.yaml` | البناء والتشغيل لخدمة Render واحدة (لم تُجرَّب على Render بعد) |
| `scripts/dev.sh`، `scripts/local-prod.sh` | التشغيل المحلي للتطوير ولمحاكاة الإنتاج |
| `docs/` | البنية ونموذج البيانات وعقد API وسجل القرارات |

### ما أُنجز في المعاينة الأولى (قبل الربط)

- [x] الرئيسية والخدمات وتفاصيل الخدمة ونموذج الطلب وصفحة النجاح والمتابعة.
- [x] صفحات التعريف والتواصل والخصوصية والشروط، مع نصوص قانونية توضيحية فقط.
- [x] البحث والتصفية والخدمات المتغيرة ونشرها وإخفاؤها محليًا.
- [x] محرر أولي لحقول النص والنص الطويل والرقم والتاريخ والاختيار الواحد.
- [x] إدارة الطلبات والملاحظات العامة والداخلية والتسعير والدفع وسجل التحديثات محليًا.
- [x] صندوق رسائل مع تصفية وحالات معالجة وملاحظات داخلية.
- [x] تحرير نصوص الرئيسية والأسئلة الشائعة والصفحات ومعاينة SEO.
- [x] مكتبة صور عامة واختيار الشعار وصور الخدمات والمقدمة داخل المعاينة.
- [x] ترتيب أقسام الرئيسية وإظهارها وتعديل روابط التنقل.
- [x] إضافة حالات الطلب وترتيبها وواجهة تنبيهات داخلية.
- [x] واجهات الفريق والإعدادات والدخول للعرض والتجربة.
- [x] نجاح بناء الإنتاج وفحص TypeScript في جولة التنفيذ السابقة.
- [x] مراجعة بعض الشاشات في المتصفح، بما فيها عرض الهاتف وإخفاء قسم ثم استعادته.

### ما أُنجز وتحقق في الشريحة الأولى (24 سبتمبر 2026)

- [x] 43 اختبارًا خلفيًا ناجحًا: المصادقة والتدوير والإبطال وفحص Origin، الصلاحيات، التحقق من النموذج، idempotency، نسخة الخدمة، الإخفاء، خصوصية المتابعة، تقييد المعدل، حماية الحذف، إبطال الكاش.
- [x] `next build` بوضع standalone ينجح دون تشغيل Django، وفحص TypeScript نظيف.
- [x] تشغيل Gunicorn وNext standalone معًا محليًا والتحقق عبر `curl` من: HTML الأولي لصفحة خدمة يحتوي المحتوى والعنوان وcanonical وJSON-LD، 404 حقيقي، تحويل 308 لـ slug قديم، خريطة الموقع وrobots، ترويسات noindex.
- [x] الدخول والتجديد ورفض المصدر غير الموثوق وإنشاء الطلب وتكراره بالمفتاح نفسه والمتابعة، كلها عبر تمرير Next.
- [x] إخفاء خدمة من API الإدارة يُرجع 404 فورًا ويزيلها من الرئيسية وخريطة الموقع، ونشرها وتعديلها يظهران خلال ثانية.
- [x] في المتصفح: الرئيسية بالتصميم المعتمد، رفض نموذج غير صحيح مع بقاء المدخلات وعرض كل الأخطاء معًا، إرسال طلب صحيح وصفحة النجاح، صفحة المتابعة تعرض الحالة والملاحظة العامة فقط، عرض الهاتف 375px دون تمرير أفقي.
- [ ] لم تُراجع شاشات لوحة التحكم بصريًا في المتصفح بعد، لأن الدخول يتطلب كلمة مرور يدخلها صاحب الحساب. وظائفها جرى التحقق منها عبر API.

### المتبقي

- [x] استبدال تنقل المعاينة المعتمد على hash بمسارات Next.js فعلية.
- [x] نقل Next.js إلى `frontend/` داخل مشروع Django وضبط `output: 'standalone'` و`rewrites` إلى Django.
- [x] تحويل الصفحات العامة إلى Server Components مع `generateMetadata` وJSON-LD.
- [x] إعداد `QueryClientProvider` و`AuthProvider` و`UiProvider` وعميل API الموحد ومفاتيح الاستعلام.
- [x] Django وDRF وPostgreSQL والترحيلات وواجهات API للشريحة الأولى.
- [x] دخول الإدارة بـ JWT (access في الذاكرة وrefresh في httpOnly cookie) والصلاحيات الفعلية.
- [x] تخزين الخادم للطلبات والخدمات والرسائل والإعدادات والأسئلة الشائعة والفريق.
- [x] روابط WhatsApp للعميل في الطلب والرسالة، وزر المنصة يظهر عند ضبط رقمها في الإعدادات.
- [ ] مراجعة بصرية لشاشات الإدارة بعد دخول صاحب الحساب.
- [ ] ربط الشاشات المتبقية من المعاينة: الوسائط، مظهر الموقع والتنقل، إدارة الحالات، الصفحات المخصصة، التنبيهات.
- [ ] رفع الملفات والتحقق منها وحماية تنزيل المرفقات الخاصة.
- [ ] عروض الأسعار وسجل الدفع وإثباته.
- [ ] تنبيهات البريد وإعادة المحاولة وسجل نجاح وفشل الإرسال.
- [ ] الاختبارات التشغيلية وتجربة الاستعادة والنشر على Render.

## 5. نطاق النسخة الأولى الكامل

### أ. واجهات العميل

الرئيسية: ترويسة وتنقل مرن، مقدمة قصيرة، زر تصفح الخدمات، متابعة طلب بارزة، المجالات والخدمات المختارة، خطوات الاستخدام، الأسئلة الشائعة، التذييل وزر WhatsApp غير حاجب للنماذج.

الخدمات: قائمة وبحث وتصفية وصفحات تفاصيل تحتوي الوصف والمتطلبات والمدة إن وُجدت وطريقة التسعير. لا تعرض إلا المحتوى المنشور.

النموذج: اسم ورقم WhatsApp أساسيان، وحقول تختلف حسب الخدمة. أنواع الحقول المستهدفة: نص قصير، نص طويل، رقم، اختيار واحد، اختيارات متعددة، تاريخ، عنوان، ملف وصورة. يضبط المدير التسمية والمساعدة والترتيب والإلزامية والخيارات والحدود ضمن الأنواع المدعومة.

النجاح والمتابعة: رقم عشوائي صعب التخمين، نسخ الرقم، صفحة متابعة تعرض اسم الخدمة والحالة وآخر تحديث والسجل العام. لا تعرض اسم العميل أو الهاتف أو إجابات النموذج أو المرفقات أو الدفع أو الملاحظات الداخلية. حامل رقم المتابعة يستطيع رؤية المعلومات العامة، لذا لا يضع المسؤول بيانات حساسة في الملاحظات العامة.

التواصل: اسم وهاتف وموضوع ورسالة، تُحفظ في النظام. أضف حالات التحميل والنجاح والفشل والفراغ وعدم العثور على الطلب والملف غير المقبول، مع حفظ المدخلات عند فشل الطلب.

### ب. لوحة التحكم

- ملخص للطلبات والرسائل الجديدة والعمل الجاري وتنبيهات التخزين والبريد.
- خدمات ومجالات: إنشاء وتحرير وترتيب ومسودة ونشر وإخفاء وصور ومتطلبات وحقول وتسعير ومدة وخدمات مختارة.
- طلبات: بحث وتصفية برقم الطلب والعميل والهاتف والخدمة والحالة والتاريخ والمسؤول؛ تفاصيل وإسناد ومرفقات وتواصل وحالات وملاحظات وسجل.
- حالات: مسميات مرنة مرتبطة بمعنى داخلي ثابت، ومنع الحذف الذي يفسد السجلات السابقة.
- تسعير: عرض سعر بقيمة وعملة، وحفظ نسخه وتسجيل موافقة العميل الخارجية وتاريخها.
- دفع: حالة مستقلة عن التنفيذ؛ غير مطلوب، بانتظار الدفع، قيد التحقق، مدفوع، مسترد. إثبات الدفع لا يعني تأكيد السداد تلقائيًا.
- رسائل: جديدة، قيد المتابعة، تمت المعالجة، مع مسؤول وملاحظات داخلية.
- محتوى: أقسام وقوالب محددة، وصفحات، وأسئلة شائعة، وقوائم، وصور وشعار وسياسات، مع مسودة ومعاينة ونشر وإخفاء وسجل تعديل.
- وسائط: مكتبة عامة مع أسماء ونصوص بديلة وإشارات الاستخدام. المرفقات الخاصة منفصلة.
- إعدادات: هوية وتواصل وروابط اجتماعية وWhatsApp وبريد وحدود ملفات وSEO.
- فريق: مدير، مشغّل، منفذ، مع صلاحيات مفعّلة على مستوى الخادم.

### ج. فجوات واجهات المعاينة التي يجب استكمالها أثناء الربط

المعاينة الحالية تغطي الاتجاه البصري، لكنها لا تشمل جميع تفاصيل النطاق التشغيلي. استكمل:

- الاختيارات المتعددة والعناوين والملفات كحقول قابلة للضبط، والرفع المتعدد وحدوده.
- فصل إعدادات السعر إلى نوع وقيمة وعملة بدل الاكتفاء بنص حر.
- حقول متطلبات الخدمة وتحديد الخدمات المختارة والترتيب والمعاينة غير المنشورة.
- مرشحات التاريخ والهاتف والمسؤول وإسناد الرسائل.
- سجل عروض الأسعار وموافقة العميل وإثباتات الدفع.
- إدارة مسودة/منشور للصفحات والأقسام، وتحرير السياسات وروابط الشبكات الاجتماعية.
- SEO لكل صفحة وخدمة، لا مجرد معاينة عامة واحدة.
- دورة الدخول والخروج، التجديد الصامت لرمز access، انتهاء refresh، والصلاحية المرفوضة (403) دون فقد المدخلات.
- حفظ رقم الطلب في رابط متابعة قابل لإعادة الفتح دون إظهار بيانات العميل.
- رسائل التحميل والفشل وإعادة المحاولة في كل عملية متصلة بالخادم.

## 6. البنية المستهدفة ونموذج البيانات

حافظ على تطبيق Next.js الحالي وانقله إلى `frontend/` داخل مشروع Django. لا تعِد تصميم الواجهات من الصفر. الهيكل المستهدف للمستودع الأحادي:

```
/
├── manage.py
├── config/              # settings (base/dev/prod)، urls، wsgi، asgi
├── apps/
│   ├── core/            # الصحة، الأخطاء، throttling، الهاتف، idempotency، وسيط الشرطة المائلة
│   ├── accounts/        # المستخدمون والأدوار ونقاط JWT
│   ├── catalog/         # المجالات والخدمات وتعريفات الحقول وإصدارات النماذج
│   ├── orders/          # الطلبات والحالات والأحداث والملاحظات والتسعير والدفع
│   ├── inquiries/       # الاستفسارات ومتابعتها
│   ├── content/         # الإعدادات والصفحات والأقسام والتنقل والأسئلة وSEO
│   ├── media_library/   # (لاحقًا) الوسائط العامة والمرفقات الخاصة وروابط التنزيل الموقّعة
│   ├── notifications/   # (لاحقًا) إشعارات البريد وسجل المحاولات
│   └── revalidation/    # استدعاء مسار إبطال كاش Next عند النشر أو التعديل
├── frontend/            # Next.js (app/ وpublic/ وpackage.json الحالية تُنقل هنا)
│   ├── app/             # المسارات
│   ├── src/
│   │   ├── lib/api/     # عميل API الموحد والتجديد التلقائي
│   │   ├── lib/query-keys.ts
│   │   ├── contexts/    # AuthContext وUiContext
│   │   ├── hooks/       # useOrders وuseServices وغيرها فوق TanStack Query
│   │   ├── lib/server-api.ts # جلب من Django داخليًا للـ Server Components
│   │   └── components/
│   ├── app/sitemap.ts وapp/robots.ts وapp/internal/revalidate/route.ts
│   └── .next/standalone/ # مخرجات البناء (غير متتبعة في git)
├── docs/
├── requirements.txt
├── render.yaml
├── build.sh             # npm ci && npm run build داخل frontend ثم collectstatic وmigrate
├── start.sh             # gunicorn على 8000 + خادم Next على $PORT
└── scripts/             # dev.sh للتطوير، local-prod.sh لمحاكاة الإنتاج محليًا
```

الكيانات المقترحة:

| المجموعة | الكيانات |
| --- | --- |
| الإدارة | مستخدم، دور/مجموعة، صلاحيات، سجل تدقيق |
| الخدمات | مجال، خدمة، تعريف حقل، إصدار نموذج |
| التشغيل | طلب، نسخة بيانات الخدمة والنموذج، إجابات، حالة، أحداث حالة، مسؤول مسند |
| الملاحظات | ملاحظة عامة، ملاحظة داخلية مع الكاتب والتاريخ |
| المال | عرض سعر وإصداره وعملته، تسجيل موافقة، سجل دفع، إثبات دفع خاص |
| التواصل | استفسار، مسؤول وحالة متابعة، إشعار بريدي ومحاولات إرساله |
| المحتوى | إعدادات الموقع، صفحة وإصدار/مسودة، قسم، عنصر تنقل، سؤال شائع، بيانات SEO |
| الملفات | وسيط عام، مرفق خاص، نوع وحجم ومسار تخزين ومالك وعلاقة بالكيان |

قواعد ملزمة:

- رقم المتابعة منفصل عن المفتاح الداخلي المتسلسل؛ قيد فريد وتوليد عشوائي مناسب.
- يحتفظ الطلب بنسخة من تعريف الخدمة والحقول ذات الصلة عند إنشائه.
- تعديل الخدمة أو إخفاؤها لا يغير الطلب القديم ولا يمنع متابعته.
- بيانات الأسعار أعداد عشرية مع عملة واضحة.
- سجل الأحداث يحتفظ بمن غيّر ماذا ومتى؛ لا تعتمد على حقل ملاحظة واحد كسجل تاريخي.
- حماية الحذف بالعلاقات والأرشفة عند وجود استخدام سابق.
- فصل واجهات API العامة عن واجهات الإدارة وعن تنزيل الملفات الخاصة.
- كل واجهات API تحت `/api/v1/` على الأصل نفسه؛ واجهات الإدارة تتطلب JWT صالحًا، والعامة تتطلب throttling.
- Django يستقبل فقط `/api/`، `/media/` العام، و`/django-admin/` (إن فُعِّل) عبر تمرير Next، ويرفض أي طلب لا يحمل ترويسة التمرير الداخلية المتفق عليها أو يأتي من غير localhost.

مجموعات API المقترحة: المحتوى العام، المجالات والخدمات، إنشاء الطلب، متابعة الطلب، إنشاء الاستفسار، مصادقة الإدارة (`auth/login`، `auth/refresh`، `auth/logout`، `auth/me`)، إدارة الموارد، المرفقات وروابط التنزيل الموقّعة، الوسائط، الإشعارات. تُوثَّق المخططات عبر drf-spectacular ويُشتق منها عميل الأنواع في الواجهة. وثّق الحقول والأخطاء والترقيم والتصفية والصلاحيات قبل ربط كل مجموعة. هذه مجموعات مقترحة وليست مسارات منفذة حاليًا.

## 7. مراحل التنفيذ ومعايير القبول

### المرحلة 0: تثبيت المرجع ومراجعة الموجود

الحالة: منجزة، عدا تقسيم مكوّنات المعاينة القديمة الذي لم يعد لازمًا لأن الشاشات المربوطة كُتبت كوحدات جديدة.

- [x] حفظ خطة المشروع في المستودع وربطها بـ README.
- [x] اعتماد قرارات البنية: نظام أحادي، Next.js داخل DRF بوضع الخادم، JWT، Context API، TanStack Query، قواعد SEO (القسم 2).
- [x] فحص الملفات وتعليمات المشروع وتحديث قائمة الفجوات.
- [x] كتابة مخطط البيانات وعقود API وسجل القرارات: `docs/data-model.md` و`docs/api-contract.md` و`docs/decisions.md` و`docs/architecture.md`.
- [x] spike: تشغيل Next standalone وGunicorn بسكربت بدء واحد محليًا، صفحة خدمة واحدة مرندرة على الخادم من API تجريبي، والتحقق من HTML الأولي و404 وتمرير cookies وإبطال الكاش.
- [x] توزيع المكونات إلى وحدات واضحة دون تغيير المظهر: الشاشات المربوطة في `frontend/src/components/`، والمعاينة القديمة محفوظة في `src/legacy/`.

المخرجات: `docs/architecture.md` (يشرح المونوليث بعمليتيه وتمرير Next إلى Django وتدفق JWT واستراتيجية الكاش وSEO) و`docs/data-model.md` و`docs/api-contract.md` و`docs/decisions.md` (مطلوبة لاحقًا، وليست ملفات موجودة بالضرورة الآن).

معيار الانتهاء: كل متطلب له مكان تنفيذ وطريقة تحقق، مع الحفاظ على الواجهات الحالية.

### المرحلة 1: استكمال الواجهات القابلة للربط

الحالة: منجزة للشاشات المربوطة. تبقى شاشات الوسائط والمظهر والحالات والصفحات المخصصة.

- [x] نقل ملفات Next.js إلى `frontend/` وضبط `output: 'standalone'` و`rewrites` و`next/font/local`. (`images.remotePatterns` يُضاف مع الوسائط.)
- [x] إنشاء مسارات فعلية: صفحات عامة كـ Server Components مع `generateMetadata` وJSON-LD و`notFound()`، وصفحات الإدارة والمتابعة كمكوّنات عميل بـ `noindex`.
- [x] `sitemap.ts` و`robots.ts` ومسار `/internal/revalidate` المحمي بسر مشترك.
- [x] إعداد `QueryClientProvider` و`AuthProvider` و`UiProvider` في `layout.tsx` وعميل API الموحد و`queryKeys`.
- [x] كتابة hooks لكل مورد (`useServices`، `useOrders`، `useInquiries`، `useContent`، `useSettings`) فوق TanStack Query، مع mutations وإبطال.
- [x] إزالة اعتماد المكونات المربوطة على localStorage. لا يوجد localStorage إلا في المعاينة القديمة المعطلة افتراضيًا.
- [x] أنواع الحقول (عدا الملف والصورة)، مرشحات الطلبات بالحالة والمسؤول والتاريخ والبحث بالرقم والاسم والهاتف، وحالات التحميل والخطأ والفراغ.
- [x] الحفاظ على RTL والهوية وlucide-react والتجاوب.

معيار الانتهاء: `next build` بوضع standalone ينجح، HTML الأولي لصفحة خدمة يحتوي المحتوى وmeta كاملة عند فحصه بـ `curl`، وكل بيانات لوحة التحكم تمر عبر TanStack Query وعميل API، ولا توجد بيانات خادم في أي Context.

### المرحلة 2: الخلفية وقاعدة البيانات

الحالة: منجزة محليًا. لم تُجرَّب سكربتات Render على Render نفسها.

- [x] إعداد مشروع Django (`config/` و`apps/`) وDRF وPostgreSQL وبيئة التطوير والترحيلات في جذر المستودع.
- [x] تطبيق `revalidation`: إشارات بعد الحفظ (post-save) للخدمات والمجالات والمحتوى والإعدادات تستدعي مسار إبطال Next بوسم المورد، مع تسجيل الفشل دون إفشال الحفظ.
- [ ] `build.sh` و`start.sh` يعملان على Render. (محليًا جرى التحقق بـ `scripts/local-prod.sh` الذي يشغّل العمليتين بالطريقة نفسها، وفحص الصحة يمر عبر Next.)
- [x] Django يستمع على 127.0.0.1 فقط و`ALLOWED_HOSTS` محصور بـ localhost، وطلبات الخادم الداخلية تحمل `X-Internal-Secret`.
- [x] إعداد متغيرات البيئة (`.env.example`)، فحص الصحة، الأخطاء الموحدة، السجلات، وdrf-spectacular.
- [x] تنفيذ الكيانات والقيود ونسخ بيانات الطلب التاريخية (للنطاق الحالي؛ كيانات المال والملفات والبريد لاحقًا).
- [x] إدارة بيانات البداية: حالات وأدوار وإعدادات أولية، مع بيانات تجريبية اختيارية ومنفصلة.

معيار الانتهاء: تشغيل متكرر موثق، ترحيلات سليمة، حفظ واسترجاع حقيقي من PostgreSQL، وفتح الموقع ولوحة التحكم وAPI من منفذ Next المكشوف وحده بعد البناء.

### المرحلة 3: المصادقة والصلاحيات

الحالة: منجزة للنطاق الحالي. روابط التنزيل الموقّعة تنتظر المرفقات.

- [x] نقاط `auth/login` و`auth/refresh` و`auth/logout` و`auth/me` بـ simplejwt، مع تدوير refresh وقائمة إبطال، وإنشاء أول مدير بأمر إدارة دون أسرار في الكود.
- [x] ضبط refresh cookie: `HttpOnly`، `Secure`، `SameSite=Strict`، مسار `/api/v1/auth/`، وفحص Origin على نقاط المصادقة.
- [x] `AuthProvider` في الواجهة: تجديد صامت عند فتح الإدارة، حفظ access في الذاكرة فقط، تجديد تلقائي عند 401 من عميل API مرة واحدة.
- [x] مدير شامل، مشغّل حسب الصلاحيات، منفذ للطلبات المسندة إليه فقط، عبر DRF permissions تقرأ الدور من قاعدة البيانات.
- [x] تطبيق صلاحيات الكيان على الخادم.
- [ ] روابط تنزيل موقّعة قصيرة الأجل للمرفقات الخاصة (مع المرحلة 5).
- [x] معالجة انتهاء refresh بنافذة دخول فوق الشاشة المفتوحة، ورفض الوصول (403) برسالة، وتسجيل أحداث الطلب باسم المنفذ.
- [x] throttling لنقاط الدخول والتجديد والواجهات العامة.
- [x] لا CORS في أي بيئة، لأن Next يمرّر API على الأصل نفسه حتى في التطوير.

معيار الانتهاء: لا يمكن تجاوز الصلاحية بتغيير الرابط أو استدعاء API مباشرة، ولا يظهر أي رمز JWT في localStorage أو في cookie قابل للقراءة من JavaScript.

### المرحلة 4: الخدمات والمحتوى والوسائط العامة

الحالة: الخدمات والمحتوى الأساسي منجزان. الوسائط والصفحات المخصصة والمعاينة المحمية للمسودات لم تُنفّذ.

- [x] CRUD حقيقي للمجالات والخدمات والحقول والترتيب والتسعير المنفصل (نوع وقيمة وعملة).
- [ ] صور الخدمات (مع الوسائط).
- [x] المسودات والنشر والإخفاء.
- [ ] المعاينة المحمية للمسودات.
- [x] ربط الرئيسية والمجالات والخدمات والإعدادات والأسئلة الشائعة بقاعدة البيانات.
- [ ] رفع الوسائط العامة والتحقق منها والنص البديل وتتبع استخدامها.
- [x] ربط SEO الفعلي: `generateMetadata` وJSON-LD وcanonical لكل صفحة وخدمة من بيانات الإدارة، تحويلات 301 عند تغيير slug، وخريطة الموقع وrobots من المحتوى المنشور فقط.
- [x] التحقق أن نشر خدمة من الإدارة يظهر في الموقع خلال ثوانٍ عبر إبطال الوسم، وأن إخفاءها يُرجع 404 وتختفي من خريطة الموقع.
- [x] استبعاد الإدارة والمتابعة والبيانات الخاصة والمسودات من الفهرسة.

معيار الانتهاء: إنشاء خدمة ونموذجها ونشرهما من الإدارة يظهر في متصفح آخر دون تعديل الكود.

### المرحلة 5: دورة الطلب والملفات الخاصة

الحالة: دورة الطلب منجزة. المرفقات والتسعير والدفع لم تُنفّذ.

- [x] إنشاء طلب بتحقق حسب إصدار نموذج الخدمة وحفظ ذري للبيانات الأساسية.
- [x] منع التكرار العرضي عند النقر أو إعادة المحاولة باستخدام آلية idempotency مناسبة.
- [ ] رفع المرفقات الخاصة وربطها بالطلب والتحقق من صلاحية تنزيلها.
- [x] إنشاء رقم المتابعة وصفحة النجاح والاستعلام العام المحدود.
- [x] إسناد وتحديث حالات وسجل أحداث وملاحظات عامة وداخلية.
- [ ] عروض الأسعار والموافقة الخارجية والدفع وإثباته.
- [x] روابط WhatsApp مع رقم منسق ونص مرمّز بأمان دون إرسال آلي.

معيار الانتهاء: طلب حقيقي من متصفح عميل يظهر في متصفح الإدارة، ويمكن تحديثه ومتابعته مع حماية بياناته الخاصة.

### المرحلة 6: الاستفسارات وتنبيهات البريد

الحالة: الاستفسارات منجزة. لا يوجد إرسال بريد بعد.

- [x] حفظ الاستفسارات وإسنادها وتحديث حالتها.
- [ ] إنشاء إشعار بعد تثبيت حفظ الطلب أو الرسالة، دون جعل نجاح الحفظ معتمدًا على البريد.
- [ ] تنبيه مختصر ورابط للإدارة دون مرفقات أو بيانات حساسة زائدة.
- [ ] سجل محاولات دائم، وإعادة محاولة محدودة بتأخير، وإعادة إرسال يدوي.
- [ ] آلية جدولة لا تفقد المحاولات عند إعادة تشغيل الخدمة ولا تكرر الإرسال دون ضبط.
- [ ] ربط قوائم التنبيهات وواجهات الفشل بمصدر البيانات الفعلي.

معيار الانتهاء: فشل البريد لا يفقد طلبًا، ويمكن تتبعه وإعادة محاولته ثم تأكيد نجاح الإرسال الفعلي عند توفر بيانات الخدمة.

### المرحلة 7: التخزين والأمان والتشغيل

الحالة: لم تبدأ.

- [ ] ربط Django بالقرص الدائم بسعة 10 GB واستخدام مسار مستقل داخل نقطة التركيب.
- [ ] فصل الوسائط العامة عن المرفقات الخاصة وعدم تقديم الخاصة بروابط عامة.
- [ ] التحقق من النوع الفعلي والامتداد والحجم والعدد وإجمالي حجم الطلب؛ أسماء داخلية عشوائية.
- [ ] قيود تمنع الملفات التنفيذية والمحتوى النشط غير المسموح، مع ضغط صور المحتوى دون إتلاف المستندات.
- [ ] مؤشر استهلاك وتنبيه قبل الامتلاء، ومعالجة الرفع الفاشل والملفات اليتيمة.
- [ ] سياسة احتفاظ معلنة لا تحذف مرفقات الطلبات المفتوحة، ودون تفعيل حذف نهائي قبل اعتمادها.
- [ ] حماية من إساءة استخدام نماذج الإرسال والمتابعة والدخول والتجديد (throttling لكل IP ولكل حساب).
- [ ] مراجعة إعدادات simplejwt للإنتاج: أعمار الرموز، خوارزمية التوقيع، مفتاح مستقل عن `SECRET_KEY` إن أمكن، وتنظيف قائمة الإبطال دوريًا.
- [ ] نسخ احتياطي واستعادة موثقان للقاعدة والملفات واختبار عملي للاستعادة.

قيد التخزين: لا تضف تخزين ملفات خارجيًا. تحقق من قدرات Render الفعلية وقت التنفيذ، بما فيها الوصول للقرص من الخدمات وحدود النسخ والتوسع. النسخة على القرص نفسه لا تحمي من فقدانه. إذا تعذرت خطة استعادة موثوقة ضمن القيد، وثّق القرار المطلوب من المالك ولا تدّعِ حل النسخ الاحتياطي.

معيار الانتهاء: ملفات محمية وحدود مساحة فعلية وسلوك واضح عند الفشل وخطة استعادة قابلة للتنفيذ.

### المرحلة 8: التحقق الشامل والتشغيل التجريبي

الحالة: توجد فحوص بناء ومراجعات واجهة سابقة فقط.

- [ ] اختبارات ذات معنى للرحلات والمخاطر الواردة في القسم 8 أدناه.
- [ ] فحص إنتاج Next.js وفحوص Django والترحيلات.
- [ ] مراجعة الهاتف والحاسوب وRTL والتباين ولوحة المفاتيح.
- [ ] تجربة خدمات مختلفة وطلبات محدودة مع الفريق بعد ضبط بيانات التشغيل.
- [ ] معالجة العوائق ومراجعة النصوص والسياسات والبيانات المنشورة.

معيار الانتهاء: اجتياز حالات القبول الحرجة، وتوثيق النتائج الفعلية دون اعتبار البيانات التجريبية دليلًا على التشغيل.

### المرحلة 9: النشر والتسليم

الحالة: لم تبدأ؛ المعاينة محلية.

- [ ] تجهيز `render.yaml` لخدمة ويب واحدة تبني بـ `build.sh` وتبدأ بـ `start.sh` (Next على `$PORT` وGunicorn داخليًا)، مع PostgreSQL والقرص الدائم والتنبيهات وخطة بذاكرة تكفي العمليتين. لا خدمة منفصلة للواجهة.
- [ ] فحص Lighthouse لصفحة خدمة وتسجيل النتيجة في `docs/test-report.md`، وتسجيل الموقع في Google Search Console بعد النشر.
- [ ] ضبط الأسرار والنطاقات وHTTPS والترحيلات والملفات الثابتة وفحوص الصحة.
- [ ] تجهيز خطة تحديث وتراجع لا تفقد بيانات العملاء.
- [ ] توثيق التشغيل وإنشاء المدير والبريد والتخزين والنسخ والاستعادة.
- [ ] إزالة حسابات الاختبار وبيانات التطوير والأسرار الافتراضية من الإنتاج.
- [ ] نشر فعلي بعد التفويض اللازم وتوفر بيانات التشغيل.
- [ ] تحقق بعد النشر من تقديم طلب وإدارته ومتابعته واستلام التنبيه.

المخرجات المطلوبة: `docs/deployment.md` و`docs/admin-guide.md` و`docs/backup-restore.md` و`docs/test-report.md` و`.env.example` دون أسرار، وتحديث README.

معيار الانتهاء: تشغيل فعلي موثق، وتسليم وصول الإدارة، وتحديد مسؤول الصيانة والنسخ والمتابعة.

## 8. مصفوفة القبول الحرجة

| الحالة | النتيجة المطلوبة |
| --- | --- |
| إنشاء مجال وخدمة جديدة | تظهر الخدمة المنشورة بنموذجها دون تعديل الكود |
| تقديم نموذج غير صحيح | رفض مفهوم مع بقاء المدخلات وعدم إنشاء طلب ناقص |
| النقر المتكرر أو إعادة الإرسال | لا ينشأ طلب مكرر بسبب إعادة المحاولة نفسها |
| تعديل الخدمة بعد الطلب | تبقى نسخة بيانات الطلب القديم كما قُدمت |
| إخفاء خدمة | تتوقف الطلبات الجديدة وتظل الطلبات القديمة قابلة للمتابعة |
| متابعة رقم صحيح | تظهر الحالة والملاحظات العامة فقط |
| رقم غير صحيح أو محاولات كثيفة | رسالة مناسبة وتقييد المحاولات دون كشف بيانات |
| محاولة منفذ فتح طلب غير مسند | رفض على مستوى الخادم |
| محاولة تنزيل مرفق دون صلاحية | رفض حتى إن عُرف معرّف المرفق أو رابطه |
| إضافة ملاحظة داخلية | لا تظهر في استجابة API العامة أو واجهة المتابعة |
| رفع إثبات دفع | لا تتحول حالة الدفع تلقائيًا إلى مدفوع |
| فشل البريد | يبقى الطلب محفوظًا ويظهر فشل قابل لإعادة المحاولة |
| إعادة تشغيل الخدمة | لا تضيع بيانات الطلبات والملفات أو سجل محاولات البريد |
| تجاوز حدود الملف أو مساحة القرص | رفض واضح دون إفساد بيانات الطلب أو ملء التخزين |
| استعادة النسخ الاحتياطية | استعادة بيانات وملفات متسقة في تجربة موثقة |
| مسودة أو صفحة خاصة | لا تظهر في القوائم العامة أو خريطة الموقع أو الفهرسة |
| عرض هاتف | لا يتجاوز المحتوى الشاشة ولا يحجب WhatsApp زر إرسال الطلب |
| انتهاء access أثناء تحرير طلب | تجديد صامت وإعادة الطلب دون فقد المدخلات أو إعادة تحميل الصفحة |
| refresh مبطل أو منتهٍ | خروج واضح ورسالة، وعدم بقاء أي بيانات إدارية في الكاش |
| البحث عن الرموز في المتصفح | لا وجود لـ JWT في localStorage أو sessionStorage أو cookie مقروء من JavaScript |
| `curl` لصفحة خدمة منشورة | HTML الأولي يحتوي الوصف والمتطلبات والعنوان وmeta وJSON-LD دون تنفيذ JavaScript |
| slug خدمة غير موجودة أو مخفية | كود 404 حقيقي وصفحة غير موجود، ولا تظهر في خريطة الموقع |
| نشر خدمة من الإدارة | تظهر في الموقع وخريطة الموقع خلال ثوانٍ دون إعادة بناء أو نشر |
| الوصول إلى منفذ Django الداخلي من الخارج | مرفوض؛ كل شيء يمر عبر منفذ Next المكشوف |
| تعديل حالة طلب من متصفح إداري | يتحدث الكاش عبر إبطال TanStack Query ويظهر التغيير في قائمة الطلبات دون تحديث يدوي |

## 9. بيانات يحتاج المالك إلى تحديدها قبل التشغيل

هذه البيانات لا تمنع تطوير الوظائف المستقلة عنها، لكنها لازمة للاختبار التشغيلي والنشر:

- الخدمات الفعلية ومجالاتها ومتطلباتها ومناطق التغطية ومددها وطريقة تسعيرها.
- رقم WhatsApp للمنصة وبيانات التواصل والهوية النهائية.
- البريد المستلم للتنبيهات وبيانات مزود الإرسال.
- وسائل الدفع وإجراءات التحقق والإلغاء والاسترداد.
- أسماء فريق الإدارة وأدوارهم ومسؤول متابعة كل نوع من الطلبات.
- حدود الملفات ومدة الاحتفاظ والنصوص النهائية للخصوصية والشروط.
- حساب Render والخطة والموارد والنطاق وبيانات DNS.
- اعتماد خطة النسخ والاستعادة وأي تعارض مع قيد التخزين.

لا تختلق بيانات تشغيل أو أسعارًا أو خدمات فعلية عند غيابها.

## 10. خارج النطاق في النسخة الأولى

- حسابات العملاء وتطبيق هاتف مستقل.
- المحادثات الداخلية أو مزامنة WhatsApp أو WhatsApp API أو الرسائل الآلية عبره.
- التتبع الحي على الخرائط.
- بوابات الدفع الإلكتروني والتسعير الآلي المعقد.
- نظام الفروع والشركاء المتعددين.
- محرر صفحات حر يولّد وظائف برمجية جديدة.
- تخزين ملفات خارجي دون تغيير صريح للقرار المعتمد.
- الخدمات المصغرة، أو خدمة واجهة منفصلة، أو مكتبات حالة عامة (Redux وZustand وغيرها)، أو مصادقة بالجلسات بدل JWT.
- التصدير الثابت لـ Next.js، وServer Actions وmiddleware في النسخة الأولى، وأي وصول من Next إلى قاعدة البيانات مباشرة.

## 11. تعليمات العمل للوكيل المنفّذ

1. اقرأ هذا الملف وREADME وتعليمات المشروع قبل التنفيذ، ثم افحص الحالة الحالية.
2. استكمل الموجود وحافظ على التصميم الذي اعتمده المستخدم؛ لا تبدأ من الصفر.
3. ابدأ بالمراجعة والعقود، ثم الأساس الخلفي والمصادقة، ثم اربط الموارد والطلبات والرسائل والملفات.
4. قدّم شريحة عمل مكتملة من الواجهة إلى قاعدة البيانات لكل وظيفة، مع بيان ما بقي تجريبيًا.
5. لا تُدخل بيانات المعاينة أو localStorage إلى الإنتاج تلقائيًا، ولا تحفظ بيانات العملاء الحساسة فيه.
6. اتخذ القرارات التنفيذية الروتينية ووثّقها. اطلب معلومات فقط حين تؤثر جوهريًا في النطاق أو التشغيل، وواصل الأجزاء المستقلة.
7. لا تعتبر كتابة مكوّن أو زر دليلًا على عمل الوظيفة. تحقّق من الرحلة والصلاحيات وحالات الفشل المناسبة.
8. حدّث مربعات هذه الخطة عند إنجاز كل مرحلة مع رابط للملفات أو تقرير تحقق، ولا تعلّم بندًا منجزًا على أساس النية فقط.
9. احفظ الأسرار خارج المستودع، ولا تكتبها في المخرجات أو ملفات التوثيق.
10. جهّز النشر والنتيجة القابلة للمراجعة قبل طلب أي تفويض خارجي لازم.
11. عند التسليم، افصل بوضوح بين ما نُفّذ وما جرى التحقق منه وما يعتمد على حسابات أو بيانات خارجية.
12. التزم بالبنية الأحادية: لا تنشئ خدمة أو مستودعًا أو عملية نشر ثانية، ولا تضع Next.js خارج مشروع Django.
13. بيانات لوحة التحكم عبر TanStack Query، والصفحات العامة عبر Server Components مع إبطال بالوسم، وحالة الواجهة عبر Context API، والمصادقة عبر JWT بالطريقة المحددة في القسم 2. أي انحراف يتطلب قرارًا موثقًا من المالك.
14. لا تعلّم بند SEO منجزًا إلا بفحص `curl` لـ HTML الأولي وتقرير Lighthouse موثق.

## 12. أول خطوة تنفيذ تالية

الشريحة الأولى منجزة (راجع القسم 4). الخطوات التالية بالترتيب:

1. يدخل صاحب المشروع إلى لوحة التحكم ويراجع شاشاتها بصريًا، ويُصلح أي ملاحظة.
2. المرحلة 5: المرفقات الخاصة وحقول الملف والصورة وروابط التنزيل الموقّعة، ثم عروض الأسعار والدفع وإثباته.
3. المرحلة 6: تنبيهات البريد بسجل محاولات دائم وإعادة محاولة.
4. المرحلة 4 المتبقية: مكتبة الوسائط وصور الخدمات، إدارة الحالات، الصفحات المخصصة والتنقل، والمعاينة المحمية للمسودات.
5. المرحلتان 7 و9: القرص الدائم والنسخ الاحتياطي، ثم النشر على Render والتحقق من `NUM_PROXIES` وترقية Next 16.

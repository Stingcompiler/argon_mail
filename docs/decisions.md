# سجل القرارات

| التاريخ | القرار | السبب |
| --- | --- | --- |
| 2026-09-24 | نظام أحادي: Django وNext في مستودع وخدمة Render واحدة، بعمليتين يشغّلهما `start.sh` | قرار المالك. يحافظ على نشر واحد ويسمح برندرة الخادم لأجل SEO |
| 2026-09-24 | Next.js بوضع `standalone` بدل التصدير الثابت | التصدير الثابت يترك HTML الخدمات فارغًا ولا يعرف الخدمات الجديدة ولا يرجع 404 |
| 2026-09-24 | Next هو المنفذ العام ويمرّر `/api` و`/media` و`/django-admin` إلى Django الداخلي | أصل واحد، بلا CORS، وDjango غير مكشوف |
| 2026-09-24 | وسيط Django يعيد الشرطة المائلة الأخيرة داخليًا | تمرير Next يحذفها، وإعادة التوجيه كانت ستدور في حلقة |
| 2026-09-24 | `NUM_PROXIES=1` | Next يمرّر XFF كما هو، وRender يضيف عنوان العميل آخرًا. يُتحقق بعد النشر |
| 2026-09-24 | JWT: access في الذاكرة، refresh في cookie محصور بمسار المصادقة، تدوير وإبطال، فحص Origin | قرار المالك مع حماية CSRF دون جلسات Django |
| 2026-09-24 | الموقع العام لا يستدعي نقاط المصادقة؛ الاستعادة تبدأ من `/admin` | تجنب استهلاك حد المعدل وطلبات 401 لكل زائر |
| 2026-09-24 | Web Locks لتسلسل التجديد بين التبويبات | الخادم يبطل refresh بعد أول استخدام |
| 2026-09-24 | سر مشترك واحد `INTERNAL_SECRET` للإبطال ولطلبات الخادم | إعفاء طلبات الخادم من التقييد وحماية مسار الإبطال بمتغير واحد |
| 2026-09-24 | المحتوى العام في Data Cache بمهلة 60 ثانية وإبطال بالوسم | حداثة فورية عند التعديل دون ضغط على Django |
| 2026-09-24 | الطلب يحفظ نسخة كاملة من الخدمة والحقول والتسميات | تعديل الخدمة لاحقًا لا يغيّر الطلبات القديمة |
| 2026-09-24 | الملاحظات سجل إضافي (OrderNote) بدل حقل واحد | الاحتفاظ بمن كتب ماذا ومتى |
| 2026-09-24 | slug عربي مسموح، وتغييره ينشئ تحويلًا دائمًا | روابط مقروءة للجمهور العربي دون كسر الروابط القديمة |
| 2026-09-24 | الخط محلي عبر fontsource و`next/font/local` | لا طلبات إلى Google وقت التشغيل، وتحسين LCP |
| 2026-09-24 | المعاينة القديمة تبقى في `src/legacy` خلف `ENABLE_DESIGN_PREVIEW` | مرجع بصري للشاشات غير المربوطة، دون localStorage في الإنتاج |
| 2026-09-24 | إبقاء Next 15.5 مؤقتًا | `npm audit` يبلغ عن PostCSS المستخدم وقت البناء، والإصلاح يتطلب Next 16 الكاسر. يُراجع في المرحلة 9 |
| 2026-09-24 | Python 3.13 على Render و3.14 محليًا | Django 6.0 يدعمهما. تُوحَّد النسخة عند إعداد النشر |
| 2026-09-24 | إرسال ملفات الطلب مع الطلب نفسه (multipart) لا رفعًا مسبقًا | لا ملفات يتيمة، والحفظ ذري مع الطلب، وidempotency تغطي الملفات |
| 2026-09-24 | التحقق من نوع الملف بالتوقيع الثنائي دون مكتبات إضافية، والأنواع PDF وPNG وJPG وWebP فقط | نطاق صغير ومعروف، ولا اعتماد على python-magic أو Pillow |
| 2026-09-24 | المرفقات الخاصة تُحفظ كما هي دون إعادة ترميز | لا يُتلف المستند الأصلي، ولا تُقدَّم داخل المتصفح (`attachment` و`sandbox`) |
| 2026-09-24 | روابط تنزيل موقّعة بـ `django.core.signing` لخمس دقائق، مربوطة بالمستخدم، مع إعادة فحص الصلاحية عند التنزيل | لا تحتاج ترويسة Authorization، وتسقط فورًا بتعطيل المستخدم أو إلغاء الإسناد |
| 2026-09-24 | حصة التخزين الخاصة 9 GB من قرص 10 GB، مع تنبيه عند 85 % وفحص المساحة الفعلية قبل الكتابة | تبقى مساحة للوسائط العامة، والرفض يحدث قبل امتلاء القرص |
| 2026-09-24 | حدود المالك للملفات في الإعدادات، وسقف ثابت على الخادم 20 MB و10 ملفات | المالك يخفض الحدود ولا يستطيع تجاوز ما يحتمله الخادم |
| 2026-09-24 | عروض الأسعار بإصدارات، والجديد يستبدل المعلّق، وقرار العميل الخارجي يُسجَّل مرة واحدة | سجل واضح لما عُرض وما وُوفق عليه |
| 2026-09-24 | الأسعار والدفع للمدير والمشغّل فقط؛ المنفذ يقرأها ويرفع المستندات | فصل المسؤولية المالية |
| 2026-09-24 | إصلاح: مزامنة حقول الخدمة تقرأ الجدول مباشرة لا كاش الجلب المسبق | كان إصدار النموذج لا يرتفع عند تعديل الحقول من لوحة التحكم |
| 2026-09-24 | تنبيهات البريد بنمط outbox داخل قاعدة البيانات وعامل ثالث في الخدمة نفسها | لا خدمة إضافية ولا Redis، والتنبيه لا يضيع ولا يؤخر حفظ الطلب |
| 2026-09-24 | `SKIP LOCKED` مع lease لعشر دقائق | لا إرسال مزدوج بين عاملين، وإعادة التقاط الصفوف إذا توقف العامل أثناء الإرسال |
| 2026-09-24 | 6 محاولات بتأخير متزايد ثم «فشل» ينتظر إعادة يدوية | يمنع إغراق مزود البريد ويُظهر المشكلة للإدارة |
| 2026-09-24 | محتوى التنبيه: رقم الطلب والخدمة والرابط فقط | البريد قناة أقل أمانًا من لوحة التحكم |
| 2026-09-24 | دون مستلم مضبوط يُسجَّل التنبيه «لم يُرسل» ويظهر شريط في لوحة التحكم | يبقى الأثر قابلًا للتتبع بدل الإسقاط الصامت |
| 2026-09-24 | حد إجمالي لملفات الطلب 20 MB وحد جسم التمرير في Next 22 MB | Next كان يقطع الأجسام عند 10 MB فيفشل الرفع بخطأ 500 |
| 2026-09-24 | وسيط Django يرفض أجسام API الكبيرة من Content-Length قبل قراءتها (JSON حتى 1 MB) | DRF يقرأ الجسم كاملًا بغض النظر عن حدود Django |
| 2026-09-24 | `is_staff`/`is_superuser` مشتقان من الدور والتفعيل في `User.save()`، ولوحة Django معطلة في الإنتاج (`ENABLE_DJANGO_ADMIN`) | خفض الرتبة كان يترك صلاحيات superuser في لوحة مكشوفة |
| 2026-09-24 | Gunicorn بخيوط (gthread، 4 خيوط) ومهلة 120 ثانية | الرفع والتنزيل البطيء كانا يقتلان العامل ويحجزان API |
| 2026-09-24 | الإنتاج يرفض البدء دون `SITE_URL` https ومضمَّن في `TRUSTED_ORIGINS`، و`INTERNAL_SECRET` قوي، و`JWT_SIGNING_KEY` منفصل | منع التشغيل الصامت بقيم محلية أو ضعيفة |
| 2026-09-24 | CSP أساسية في الإنتاج مع `'unsafe-inline'` للسكربتات | Next يحقن سكربتات مضمنة؛ الـ nonce يحتاج middleware مؤجلًا |
| 2026-09-24 | `preedargon@gmail.com` مستلم افتراضي للتنبيهات (`DEFAULT_ALERT_EMAILS`) حتى تُضبط من لوحة التحكم | طلب المالك؛ لا تُفقد تنبيهات الأيام الأولى |
| 2026-09-24 | CI على GitHub Actions: خلفية على Postgres 16 وPython 3.13، واجهة على Node 22، إشراف `start.sh`، ودخان من طرف إلى طرف مع Lighthouse | لا يُدمج شيء دون فحص؛ Python 3.13 مطابق لـ Render |
| 2026-09-24 | تحذير npm audit للمستوى high (ثغرة PostCSS في Next 15) لا يُفشل CI، والمستوى critical يُفشله | الترقية إلى Next 16 مؤجلة ومقصودة |
| 2026-09-24 | الترحيلات في `preDeployCommand` لا في البناء | فشل البناء لا يترك القاعدة مرحّلة |
| 2026-09-24 | فحص الصحة يفشل عند عامل بريد متوقف (نبضة أقدم من 3 دقائق) أو قرص غير مركّب، على Render فقط | Render يعيد تشغيل الخدمة بدل عمل صامت ناقص |
| 2026-09-24 | Sentry اختياري للخلفية فقط، دون بيانات شخصية | يعمل عند ضبط `SENTRY_DSN`؛ الواجهة لاحقًا |
| 2026-09-24 | نسخ متسقة: القاعدة أولًا ثم الملفات، واستعادة تنتهي بـ `reconcile_files --checksums` | لقطات Render للقرص والقاعدة غير متزامنة |
| 2026-09-24 | حقل هاتف العميل: اختيار الدولة بالاسم العربي (السودان افتراضيًا) ويُضاف مفتاحها تلقائيًا، وتُحذف الأصفار البادئة | طلب المالك؛ يقلل أخطاء إدخال رمز الدولة. الأسماء من `Intl.DisplayNames` والرموز من جدول E.164 في `frontend/src/lib/countries.ts` |
| 2026-09-24 | المتابعة بالاسم الكامل تتطلب رقم WhatsApp المستخدم في الطلب أيضًا | الاسم وحده (مثل «محمد أحمد») يكشف طلبات أشخاص آخرين؛ طلب المالك نُفّذ بهذا القيد. المطابقة توحّد أ/إ/آ وى/ي وة/ه والتشكيل والمسافات |
| 2026-09-24 | البحث بالاسم عبر POST ومعدل 5/دقيقة، ورد «غير موجود» واحد سواء أخطأ الاسم أو الهاتف | يُبقي البيانات الشخصية خارج الروابط والسجلات ويصعّب التخمين |
| 2026-09-24 | تنبيهات الإدارة عبر Gmail (`preedargon@gmail.com`) بكلمة مرور تطبيق، محليًا في `.env` وفي الإنتاج عبر `EMAIL_URL` السري في Render | اختيار المالك؛ لا أسرار في المستودع |
| 2026-09-24 | تنبيه رسالة التواصل يحمل محتوى الرسالة كاملًا (الاسم والهاتف والموضوع والنص) بقالب HTML منسق | قرار المالك للرد السريع دون فتح لوحة التحكم. يستبدل قرار «التنبيه دون بيانات» للرسائل فقط؛ تنبيه الطلبات يبقى مختصرًا. المحتوى يُهرَّب في HTML والموضوع سطر واحد لمنع حقن الترويسات |
| 2026-09-24 | الدخول بالبريد أو اسم مستخدم اختياري فريد (أحرف صغيرة) | طلب المالك. البريد يبقى المعرّف الأساسي؛ اسم المستخدم مجرد اسم بديل، والخطأ موحّد لمنع كشف الحسابات |
| 2026-09-25 | ترتيب صريح `order_by("sort_order","id")` في كل استعلام يستخدم `annotate(Count)` | Django يتجاهل `Meta.ordering` في الاستعلامات المجمّعة، فكانت قوائم الحالات والمجالات والخدمات تظهر بلا ترتيب |
| 2026-09-25 | لوحة الإدارة تعرض «تعذّر الوصول إلى الخادم» بزر إعادة عند فشل الشبكة أو 5xx أو 429، ولا تُخرج المستخدم إلا عند 401/403 | كانت تدور للأبد عند انقطاع الشبكة، وتُخرج المستخدم عند خطأ خادم عابر |
| 2026-09-25 | مكدس نوافذ: النافذة العليا وحدها تستجيب لـ Escape وTab | Escape في نافذة «انتهت الجلسة» كان يغلق نافذة الطلب تحتها ويضيّع المسودة |
| 2026-09-25 | الصفحات (الخصوصية والشروط وصفحات مخصصة على `/p/<slug>`) نص عادي: سطر فارغ بين الفقرات و«## » للعناوين، دون HTML | تحرير بسيط وآمن من الإدارة بلا حقن؛ صفحتا السياسات ثابتتا الرابط ومنشورتان دائمًا لأن نموذج الطلب يربط بهما |
| 2026-09-25 | صورة المشاركة والأيقونات تُرسم وقت البناء بـ `next/og` وخط Cairo، وكل كلمة عربية في عنصر مستقل | محرك الرسم لا يقرأ جداول Noto Sans Arabic ولا يرتّب الكلمات من اليمين |
| 2026-09-25 | الصور العامة تُعاد ترميزها بـ Pillow (تصغير إلى 2000px، إزالة EXIF، رفض الصور الضخمة جدًا)، وSVG غير مقبول | حماية الخصوصية (موقع التصوير) والأداء، وSVG قد يحمل سكربتات |
| 2026-09-25 | `/media/` يقدّم `assets/` فقط بأسماء عشوائية وتخزين سنة `immutable`، والمرفقات الخاصة خارج هذا المسار تمامًا | لا طريق من الرابط العام إلى الملفات الخاصة |
| 2026-09-25 | الصورة المستخدمة (خدمة أو شعار أو مقدمة) لا تُحذف، والنص البديل إلزامي | لا روابط مكسورة في الموقع، ووصولية وSEO |
| 2026-09-25 | معاينة المسودات برابط موقّع مربوط بنوع العنصر ومعرّفه، صالح 30 دقيقة، دون كاش ومع `noindex` | الجلسة في المتصفح لا تصل إلى الخادم الذي يرندر الصفحة؛ الرابط الموقّع يعمل دون كشف المسودات للعموم |
| 2026-09-25 | روابط القائمة داخلية فقط (تبدأ بـ `/` وليست `//` ولا مخططًا)، حتى 8 روابط | منع روابط خارجية أو `javascript:` في رأس الموقع |
| 2026-09-25 | Playwright على Chromium فقط: رحلة العميل على هاتف (Pixel 7) ولوحة التحكم على سطح المكتب، ضد خادم الإنتاج المحلي نفسه في CI | يغطي صفوف القبول التي تحتاج متصفحًا حقيقيًا بأقل وقت تشغيل؛ انتهاء الجلسة يُحاكى باعتراض الطلبات بدل الانتظار |
| 2026-09-25 | مستخدم e2e في CI بكلمة مرور عشوائية لكل تشغيل، وبريده في النطاق المحجوز `.invalid` | لا أسرار ثابتة في المستودع، ولا بريد حقيقي يستقبل تنبيهات |
| 2026-09-25 | مزوّد المصادقة في لوحة التحكم فقط، وTanStack Query في الإدارة وفي الصفحات العامة التي فيها نموذج فقط | الصفحات العامة الأخرى HTML من الخادم ولا تحتاجهما؛ أقل JS يعني LCP أسرع على شبكات الجوال |
| 2026-09-25 | أخطاء الحقول مربوطة بـ `aria-describedby` دون `role="alert"`، والتركيز ينتقل إلى أول حقل خاطئ؛ رسالة عامة واحدة فقط تُعلن | قارئ الشاشة يقرأ الخطأ عند الوصول إلى الحقل، بدل إعلان كل الأخطاء دفعة واحدة |
| 2026-09-25 | axe-core يعمل داخل Playwright على كل صفحة عامة وكل شاشة إدارة، وأي مخالفة WCAG AA أو خلل في ترتيب العناوين يُفشل CI | التباين والوصولية لا يتراجعان بصمت مع تعديلات التصميم |
